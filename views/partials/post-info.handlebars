<article class="post">
  <div class="title">
    <h2>{{title}}</h2>
  </div>
  <div class="content">
    <h3>{{content}}</h3>
    <a href="/post/{{id}}">{{comment.length}}
      {{format_plural "comment" comment.length}}</a>
  </div>
</article>